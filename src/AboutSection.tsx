function AboutSection() {
    return (
        <section aria-labelledby="about-section-heading">
            <h1 id="about-section-heading">About Section</h1>
            <p>This is the About Section component.</p>
        </section>
    );
};

export default AboutSection;